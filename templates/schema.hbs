import { EntitySchema } from 'typeorm';
import { BaseSchemaProperties } from './base.schema';
import { {{ModuleName}} } from '../../../entities/{{moduleNameKebab}}.entity';

export const {{moduleNamePascal}}Schema = new EntitySchema<{{moduleNamePascal}}>({
    name: {{moduleNamePascal}}.name,
    target: {{moduleNamePascal}},
    tableName: '{{moduleNamePlural}}',
    columns: {
        ...BaseSchemaProperties,
    },
});
